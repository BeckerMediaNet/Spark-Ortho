# Secrets / environment-specific config — never commit
wp-config.php
.vscode/
_wpeprivate/
/sftp.json

# WP Engine / OS noise
.DS_Store
*.log

# Node dependencies
node_modules/

# Editor backup files created during this project's CSS cleanup
*.bak
