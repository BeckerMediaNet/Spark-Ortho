/******/ (() => { // webpackBootstrap
/*!***************************************************!*\
  !*** ./blocks/dentist-cards/src/dentist-cards.js ***!
  \***************************************************/
function _typeof(o) { "@babel/helpers - typeof"; return _typeof = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function (o) { return typeof o; } : function (o) { return o && "function" == typeof Symbol && o.constructor === Symbol && o !== Symbol.prototype ? "symbol" : typeof o; }, _typeof(o); }
function ownKeys(e, r) { var t = Object.keys(e); if (Object.getOwnPropertySymbols) { var o = Object.getOwnPropertySymbols(e); r && (o = o.filter(function (r) { return Object.getOwnPropertyDescriptor(e, r).enumerable; })), t.push.apply(t, o); } return t; }
function _objectSpread(e) { for (var r = 1; r < arguments.length; r++) { var t = null != arguments[r] ? arguments[r] : {}; r % 2 ? ownKeys(Object(t), !0).forEach(function (r) { _defineProperty(e, r, t[r]); }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : ownKeys(Object(t)).forEach(function (r) { Object.defineProperty(e, r, Object.getOwnPropertyDescriptor(t, r)); }); } return e; }
function _defineProperty(e, r, t) { return (r = _toPropertyKey(r)) in e ? Object.defineProperty(e, r, { value: t, enumerable: !0, configurable: !0, writable: !0 }) : e[r] = t, e; }
function _classCallCheck(a, n) { if (!(a instanceof n)) throw new TypeError("Cannot call a class as a function"); }
function _defineProperties(e, r) { for (var t = 0; t < r.length; t++) { var o = r[t]; o.enumerable = o.enumerable || !1, o.configurable = !0, "value" in o && (o.writable = !0), Object.defineProperty(e, _toPropertyKey(o.key), o); } }
function _createClass(e, r, t) { return r && _defineProperties(e.prototype, r), t && _defineProperties(e, t), Object.defineProperty(e, "prototype", { writable: !1 }), e; }
function _toPropertyKey(t) { var i = _toPrimitive(t, "string"); return "symbol" == _typeof(i) ? i : i + ""; }
function _toPrimitive(t, r) { if ("object" != _typeof(t) || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != _typeof(i)) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }
(function ($) {
  "use strict";

  var DentistFilter = /*#__PURE__*/function () {
    function DentistFilter($block) {
      _classCallCheck(this, DentistFilter);
      this.$block = $block;
      this.$grid = $block.find(".dentist-cards-flex");
      this.$loadMoreBtn = $block.find(".load-more-dentists");
      this.$resetBtn = $block.find(".filter-reset");
      this.$spinner = $block.find(".dentist-spinner");
      this.$filteredDiv = $block.find(".filtered");
      this.perPage = parseInt($block.data("per-page")) || 12;
      this.imageFit = $block.data("image-fit") || "cover";
      this.currentPage = 1;
      this.isLoading = false;
      this.currentFilters = {};
      this.init();
    }
    return _createClass(DentistFilter, [{
      key: "init",
      value: function init() {
        var _this = this;
        this.loadDentists(false);

        // Filter submit button
        this.$block.find(".filter-submit").on("click", function (e) {
          e.preventDefault();

          // Handle keyword input
          var keywordInput = _this.$block.find('input[name="keyword"]');
          var keywordValue = keywordInput.val().trim();
          if (keywordValue) {
            var existingKeywordFilter = _this.$filteredDiv.find('.btn-filter[data-filter-type="keyword"]');
            if (existingKeywordFilter.length === 0) {
              _this.addFilter("keyword", keywordValue, "Keyword: \"".concat(keywordValue, "\""));
            }
          }
          _this.currentPage = 1;
          _this.loadDentists(false);
        });
        this.$resetBtn.on("click", function (e) {
          e.preventDefault();
          _this.resetFilters();
        });
        this.$loadMoreBtn.on("click", function (e) {
          e.preventDefault();
          _this.loadMore();
        });

        // Dropdown filter items
        this.$block.find(".dropdown-item").on("click", function (e) {
          e.preventDefault();
          var $item = $(e.target);
          var filterType = $item.data("filter-type");
          var filterValue = $item.data("filter-value");
          var filterText = $item.text().trim();
          _this.addFilter(filterType, filterValue, filterText);
        });

        // Keyword input
        this.$block.find('input[name="keyword"]').on("keypress", function (e) {
          if (e.key === "Enter") {
            e.preventDefault();
            var keyword = $(e.target).val().trim();
            if (keyword) {
              _this.addFilter("keyword", keyword, keyword);
            }
          }
        });

        // Remove filter buttons (delegated event)
        this.$filteredDiv.on("click", ".btn-filter", function (e) {
          e.preventDefault();
          _this.removeFilter($(e.target));
        });
      }
    }, {
      key: "addFilter",
      value: function addFilter(type, value, text) {
        // Check if filter already exists
        var existingFilter = this.$filteredDiv.find("[data-filter-type=\"".concat(type, "\"][data-filter-value=\"").concat(value, "\"]"));
        if (existingFilter.length === 0) {
          var filterBtn = $("<button class=\"btn-filter\" data-filter-type=\"".concat(type, "\" data-filter-value=\"").concat(value, "\">").concat(text, "</button>"));
          this.$filteredDiv.append(filterBtn);
        }

        // Show reset button
        this.$resetBtn.removeClass("d-none");
        this.currentPage = 1;
        this.loadDentists(false);
      }
    }, {
      key: "removeFilter",
      value: function removeFilter($filterBtn) {
        var filterType = $filterBtn.data("filter-type");
        $filterBtn.remove();

        // Hide reset button if no filters remain
        if (this.$filteredDiv.find(".btn-filter").length === 0) {
          this.$resetBtn.addClass("d-none");
        }

        // Clear keyword input if it was a keyword filter
        if (filterType === "keyword") {
          this.$block.find('input[name="keyword"]').val("");
        }
        this.currentPage = 1;
        this.loadDentists(false);
      }
    }, {
      key: "resetFilters",
      value: function resetFilters() {
        this.$filteredDiv.empty();
        this.$block.find('input[name="keyword"]').val("");
        this.$resetBtn.addClass("d-none");
        this.currentPage = 1;
        this.loadDentists(false);
      }
    }, {
      key: "displayNoResultsMessage",
      value: function displayNoResultsMessage(filtersApplied) {
        var message = '<div class="col-12"><div class="no-results-message text-center py-5">';
        if (filtersApplied && filtersApplied.has_keyword && filtersApplied.keyword) {
          // Specific message for keyword searches
          message += "<h4>No dentists found</h4>";
          message += "<p class=\"mb-3\">No dentists match the keyword \"<strong>".concat(filtersApplied.keyword, "</strong>\"");

          // Add additional filter context if other filters are applied
          if (filtersApplied.specialty && filtersApplied.specialty.length > 0) {
            message += " with the selected specialties";
          }
          if (filtersApplied.location && filtersApplied.location.length > 0) {
            message += " at the selected locations";
          }
        } else if (filtersApplied && filtersApplied.has_filters) {
          message += "<h4>No dentists found</h4>";
          message += "<p class=\"mb-3\">No dentists match your current filter selection.</p>";
        } else {
          message += "<h4>No dentists found</h4>";
          message += "<p>Please check back later or contact us for assistance.</p>";
        }
        message += "</div></div>";
        this.$grid.html(message);
      }
    }, {
      key: "collectActiveFilters",
      value: function collectActiveFilters() {
        var filters = {};

        // Collect filter buttons
        this.$filteredDiv.find(".btn-filter").each(function () {
          var $btn = $(this);
          var type = $btn.data("filter-type");
          var value = $btn.data("filter-value");
          if (!filters[type]) {
            filters[type] = [];
          }
          filters[type].push(value);
        });

        // Also check the keyword input field directly
        var keywordInput = this.$block.find('input[name="keyword"]');
        if (keywordInput.length && keywordInput.val().trim()) {
          filters.keyword = keywordInput.val().trim();
        }
        return filters;
      }
    }, {
      key: "loadMore",
      value: function loadMore() {
        this.currentPage++;
        this.loadDentists(true);
      }
    }, {
      key: "loadDentists",
      value: function loadDentists() {
        var _this2 = this;
        var append = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : false;
        if (this.isLoading) return;

        // Check if AJAX variables are available
        if (typeof dentist_cards_ajax === "undefined") {
          console.error("DentistFilter: dentist_cards_ajax is not defined. Check script localization.");
          this.$grid.html('<div class="col-12"><p class="text-center text-danger">Configuration error. Please check script localization.</p></div>');
          return;
        }
        this.isLoading = true;
        this.$spinner.removeClass("d-none");
        if (!append) {
          this.$grid.html("");
        }
        var activeFilters = this.collectActiveFilters();
        var data = _objectSpread({
          action: "filter_dentists",
          paged: this.currentPage,
          per_page: this.perPage,
          image_fit: this.imageFit,
          nonce: dentist_cards_ajax.nonce
        }, activeFilters);
        $.ajax({
          url: dentist_cards_ajax.ajax_url,
          type: "POST",
          data: data,
          success: function success(response) {
            // Handle response.html format (matches testimonials pattern)
            if (response.html) {
              if (append) {
                _this2.$grid.append(response.html);
              } else {
                _this2.$grid.html(response.html);
              }
            } else {
              console.error("No HTML content in response:", response);
            }
            var count = response.total_posts || 0;

            // Handle no results case with specific messages
            if (count === 0 && !append) {
              _this2.displayNoResultsMessage(response.filters_applied);
            }

            // Show/hide load more button based on whether there are more pages
            if (response.has_more) {
              _this2.$loadMoreBtn.removeClass("d-none");
            } else {
              _this2.$loadMoreBtn.addClass("d-none");
            }

            // Update results count if element exists
            var $resultsCount = _this2.$block.find(".results-count");
            if ($resultsCount.length && count !== undefined) {
              $resultsCount.text("Showing ".concat(count, " dentists"));
            }
            _this2.isLoading = false;
            _this2.$spinner.addClass("d-none");
          },
          error: function error(xhr, status, _error) {
            console.error("AJAX Error:", _error);
            console.error("Status:", status);
            console.error("Response Text:", xhr.responseText);
            _this2.isLoading = false;
            _this2.$spinner.addClass("d-none");
            _this2.$grid.html('<div class="col-12"><p class="text-center text-danger">Error loading dentists. Please try again.</p></div>');
          }
        });
      }
    }]);
  }();
  $(document).ready(function () {
    var $blocks = $(".dentist-cards-container[data-mode='all']");
    $blocks.each(function () {
      new DentistFilter($(this));
    });
  });
})(jQuery);
/******/ })()
;