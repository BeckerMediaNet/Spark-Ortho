/******/ (() => { // webpackBootstrap
/*!*************************************************!*\
  !*** ./blocks/podcast-grid/src/podcast-grid.js ***!
  \*************************************************/
function _typeof(o) { "@babel/helpers - typeof"; return _typeof = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function (o) { return typeof o; } : function (o) { return o && "function" == typeof Symbol && o.constructor === Symbol && o !== Symbol.prototype ? "symbol" : typeof o; }, _typeof(o); }
function _classCallCheck(a, n) { if (!(a instanceof n)) throw new TypeError("Cannot call a class as a function"); }
function _defineProperties(e, r) { for (var t = 0; t < r.length; t++) { var o = r[t]; o.enumerable = o.enumerable || !1, o.configurable = !0, "value" in o && (o.writable = !0), Object.defineProperty(e, _toPropertyKey(o.key), o); } }
function _createClass(e, r, t) { return r && _defineProperties(e.prototype, r), t && _defineProperties(e, t), Object.defineProperty(e, "prototype", { writable: !1 }), e; }
function _toPropertyKey(t) { var i = _toPrimitive(t, "string"); return "symbol" == _typeof(i) ? i : i + ""; }
function _toPrimitive(t, r) { if ("object" != _typeof(t) || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != _typeof(i)) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }
document.addEventListener("DOMContentLoaded", function () {
  var blocks = document.querySelectorAll(".block--podcast-grid");
  if (!blocks.length) return;
  blocks.forEach(function (block) {
    new PodcastGrid(block);
  });
  var PodcastGrid = /*#__PURE__*/function () {
    function PodcastGrid(element) {
      _classCallCheck(this, PodcastGrid);
      this.block = element;
      this.blockId = this.block.dataset.blockId;
      this.grid = this.block.querySelector(".podcast-grid");
      this.cards = this.grid ? Array.from(this.grid.querySelectorAll(".podcast-card")) : [];
      this.searchInput = this.block.querySelector(".podcast-search-input");
      this.clearSearchBtn = this.block.querySelector(".clear-search");
      this.categoryFilter = this.block.querySelector(".podcast-category-filter");
      this.resultsCount = this.block.querySelector(".results-count .count");
      this.resultsTotal = this.block.querySelector(".results-count .total");
      this.noResultsMsg = this.block.querySelector(".no-results");
      this.searchTerm = "";
      this.selectedCategory = "";
      this.init();
    }
    return _createClass(PodcastGrid, [{
      key: "init",
      value: function init() {
        var _this = this;
        if (this.searchInput) {
          this.setupSearch();
        }
        if (this.categoryFilter) {
          this.setupCategoryFilter();
        }

        // Store original card data for filtering
        this.cardData = this.cards.map(function (card) {
          var _card$querySelector, _card$querySelector2;
          return {
            element: card,
            title: ((_card$querySelector = card.querySelector(".card-title")) === null || _card$querySelector === void 0 ? void 0 : _card$querySelector.textContent.toLowerCase()) || "",
            excerpt: ((_card$querySelector2 = card.querySelector(".card-excerpt")) === null || _card$querySelector2 === void 0 ? void 0 : _card$querySelector2.textContent.toLowerCase()) || "",
            tags: Array.from(card.querySelectorAll(".tag")).map(function (tag) {
              return tag.textContent.toLowerCase();
            }),
            categories: _this.getCategoryIds(card)
          };
        });
      }
    }, {
      key: "setupSearch",
      value: function setupSearch() {
        var _this2 = this;
        var searchTimeout;
        this.searchInput.addEventListener("input", function (e) {
          clearTimeout(searchTimeout);

          // Debounce search for better performance
          searchTimeout = setTimeout(function () {
            _this2.searchTerm = e.target.value.toLowerCase().trim();
            _this2.filterCards();
            _this2.toggleClearButton();
          }, 300);
        });

        // Clear search button
        if (this.clearSearchBtn) {
          this.clearSearchBtn.addEventListener("click", function () {
            _this2.searchInput.value = "";
            _this2.searchTerm = "";
            _this2.clearSearchBtn.style.display = "none";
            _this2.filterCards();
            _this2.searchInput.focus();
          });
        }

        // Clear on Escape key
        this.searchInput.addEventListener("keydown", function (e) {
          if (e.key === "Escape") {
            _this2.searchInput.value = "";
            _this2.searchTerm = "";
            _this2.toggleClearButton();
            _this2.filterCards();
          }
        });
      }
    }, {
      key: "setupCategoryFilter",
      value: function setupCategoryFilter() {
        var _this3 = this;
        this.categoryFilter.addEventListener("change", function (e) {
          _this3.selectedCategory = e.target.value;
          _this3.filterCards();
        });
      }
    }, {
      key: "toggleClearButton",
      value: function toggleClearButton() {
        if (!this.clearSearchBtn) return;
        if (this.searchInput.value.length > 0) {
          this.clearSearchBtn.style.display = "flex";
        } else {
          this.clearSearchBtn.style.display = "none";
        }
      }
    }, {
      key: "getCategoryIds",
      value: function getCategoryIds(card) {
        // This would need to be populated from PHP if we want category filtering
        // For now, we'll use a data attribute approach
        var categoryAttr = card.dataset.categories;
        return categoryAttr ? categoryAttr.split(",").map(function (id) {
          return id.trim();
        }) : [];
      }
    }, {
      key: "filterCards",
      value: function filterCards() {
        var _this4 = this;
        var visibleCount = 0;
        this.cardData.forEach(function (data) {
          var matches = true;

          // Search filter
          if (_this4.searchTerm) {
            var searchMatches = data.title.includes(_this4.searchTerm) || data.excerpt.includes(_this4.searchTerm) || data.tags.some(function (tag) {
              return tag.includes(_this4.searchTerm);
            });
            matches = matches && searchMatches;
          }

          // Category filter
          if (_this4.selectedCategory) {
            var categoryMatches = data.categories.includes(_this4.selectedCategory);
            matches = matches && categoryMatches;
          }

          // Show/hide card with animation
          if (matches) {
            data.element.classList.remove("hidden");
            data.element.style.display = "";
            visibleCount++;

            // Fade in animation
            setTimeout(function () {
              data.element.style.opacity = "1";
              data.element.style.transform = "translateY(0)";
            }, 10);
          } else {
            data.element.style.opacity = "0";
            data.element.style.transform = "translateY(10px)";
            setTimeout(function () {
              data.element.classList.add("hidden");
            }, 300);
          }
        });

        // Update results count
        this.updateResultsCount(visibleCount);

        // Show/hide no results message
        this.toggleNoResults(visibleCount === 0);
      }
    }, {
      key: "updateResultsCount",
      value: function updateResultsCount(visibleCount) {
        if (this.resultsCount) {
          this.resultsCount.textContent = visibleCount;
        }
      }
    }, {
      key: "toggleNoResults",
      value: function toggleNoResults(show) {
        if (!this.noResultsMsg) return;
        if (show) {
          this.noResultsMsg.style.display = "block";
          if (this.grid) {
            this.grid.style.display = "none";
          }
        } else {
          this.noResultsMsg.style.display = "none";
          if (this.grid) {
            this.grid.style.display = "grid";
          }
        }
      }

      // Public method to reset filters
    }, {
      key: "reset",
      value: function reset() {
        if (this.searchInput) {
          this.searchInput.value = "";
        }
        if (this.categoryFilter) {
          this.categoryFilter.value = "";
        }
        this.searchTerm = "";
        this.selectedCategory = "";
        this.toggleClearButton();
        this.filterCards();
      }
    }]);
  }(); // Expose to global scope if needed for external controls
  window.PodcastGrid = PodcastGrid;
});
/******/ })()
;